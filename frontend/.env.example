# Base URL of the Recipe AI backend API.
# Defaults to http://localhost:8000/api if unset.
VITE_API_URL=http://localhost:8000/api

# Google OAuth *Web* client id for "Sign in with Google" (public; no secret).
# Leave empty to hide the Google button entirely.
VITE_GOOGLE_CLIENT_ID=
